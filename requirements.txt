# manta-hic dependencies.
#
# POLICY: no backwards compatibility. We do NOT support old Python or old library versions — newest is
# preferred, especially when it keeps the code simpler. These are modern *minimum* floors only: no upper
# caps, no legacy pins, no compatibility shims. Requires Python >= 3.12.
#
# Install note: these are all plain `pip install -r`-able (no build hacks). k-let sequence shuffling is
# pure-Python (manta_hic/ops/kshuffle.py), so the old `ushuffle` C dependency is gone. numba is NOT imported
# by manta_hic -- it is a resolver pin: cooltools requires `numba` unversioned, and without a floor the
# solver backtracks to numba 0.53 (won't build on 3.12, incompatible with numpy 2). >=0.60 forces a coherent
# modern numba/numpy stack.

torch>=2.6
numpy>=2.0
pandas>=2.2
polars>=1.0
pysam>=0.23
click>=8.1
h5py>=3.11
hdf5plugin>=4.4
bioframe>=0.7
cooler>=0.10
cooltools>=0.7
numba>=0.60
